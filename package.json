{
	"name": "test",
	"version": "0.0.1",
	"private": true,
	"main": "index.js",
	"dependencies": {
		"http-server": "^0.6.1",
		"bower": "~1.3.12",
		"grunt-cli": "~0.1.13"
	},

	"scripts": {
		"postinstall": "bower install",
    	"prestart": "npm install",
		"test": "echo \"Error: no test specified\" && exit 1"
	}
}