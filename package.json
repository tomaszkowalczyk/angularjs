{
	"name": "test",
	"version": "0.0.1",
	"private": true,
	"main": "index.js",
	"dependencies": {
		"ejs": "^2.3.1",
    	"express": "~4.9.x",
		"http-server": "^0.6.1",
		"bower": "~1.3.12",
		"grunt-cli": "~0.1.13"
	},
	"engines": {
		"node": "0.12.2"
	},
	"scripts": {
		"postinstall": "bower install",
    	"prestart": "npm install",
		"test": "echo \"Error: no test specified\" && exit 1"
	}
}